# Changelog

All notable changes to TekVwarho ProAudit will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

[cliff]
# Template for the changelog body
# https://keats.github.io/tera/docs/#introduction
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | upper_first }}
    {% for commit in commits %}
        - {% if commit.breaking %}[**BREAKING**] {% endif %}{{ commit.message | upper_first }}\
    {% endfor %}
{% endfor %}\n
"""

# Parse commits and modify them
[git]
# Parse GitHub-style commits
conventional_commits = true
filter_unconventional = true
split_commits = false

# Regex for preprocessing the commit messages
commit_preprocessors = [
    # Remove issue numbers from message
    { pattern = '\\(#([0-9]+)\\)', replace = "" },
]

# Group commits by their type
commit_parsers = [
    { message = "^feat", group = "🚀 Features" },
    { message = "^fix", group = "🐛 Bug Fixes" },
    { message = "^doc", group = "📚 Documentation" },
    { message = "^perf", group = "⚡ Performance" },
    { message = "^refactor", group = "🔨 Refactoring" },
    { message = "^style", group = "🎨 Styling" },
    { message = "^test", group = "🧪 Testing" },
    { message = "^chore\\(release\\)", skip = true },
    { message = "^chore\\(deps\\)", group = "📦 Dependencies" },
    { message = "^chore\\(ci\\)", group = "⚙️ CI/CD" },
    { message = "^chore", group = "🔧 Miscellaneous" },
    { message = "^ci", group = "⚙️ CI/CD" },
    { message = "^security", group = "🔐 Security" },
    { body = ".*security", group = "🔐 Security" },
]

# Protect tags
protect_breaking_commits = false
filter_commits = false

# Tag pattern
tag_pattern = "v[0-9]*"

# Ignore tags
ignore_tags = ""

# Sort order
topo_order = false
sort_commits = "oldest"
